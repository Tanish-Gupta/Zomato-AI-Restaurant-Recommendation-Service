# Core dependencies
datasets>=2.16.0
pandas>=2.0.0
numpy>=1.24.0

# API Framework (for later phases)
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.5.0

# LLM Integration (for later phases)
groq>=0.4.0

# Pin for Gradio compatibility (Gradio 4.x imports HfFolder, removed in hf_hub 1.0)
huggingface_hub>=0.23,<1.0

# UI (for later phases)
gradio>=4.0.0

# Utilities
python-dotenv>=1.0.0
httpx>=0.26.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0

# Development
ruff>=0.1.0
